# Install devDependencies during builds even when NODE_ENV=production.
# The Next.js production build needs typescript (tsconfig path aliases),
# tailwindcss, postcss and autoprefixer — all devDependencies. Without this,
# Render's production install omits them and the build fails.
include=dev
